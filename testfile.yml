---
- hosts: linux
  tasks:
  - name: Create training file

    file:

     path: /home/ansible/training

     state: touch
  - name: Create training  Directory
    file:
      path: /home/ansible/trainingdir
      state: directory

